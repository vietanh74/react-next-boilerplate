import type { ThemeConfig } from 'antd';

export const theme: ThemeConfig = {
  token: {
    fontFamily: 'var(--font-family-base)',
    colorPrimary: '#069255',
    fontSize: 16,
    borderRadius: 4,
  },
};
